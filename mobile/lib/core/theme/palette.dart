import 'package:flutter/material.dart';

/// The brand palette, carried over from the web storefront so the app and the
/// site read as one thing.
abstract final class Brand {
  static const gold = Color(0xFFC9A227);
  static const charcoal = Color(0xFF2D2D2D);
  static const cream = Color(0xFFFAF8F5);

  static const ink = Color(0xFF141414);
  static const inkMuted = Color(0xFF6B6B6B);

  static const paperDark = Color(0xFF0E0E0E);
  static const surfaceDark = Color(0xFF1A1A1A);
  static const inkDark = Color(0xFFF2F0ED);
  static const inkMutedDark = Color(0xFF9A9A9A);

  static const hairline = Color(0x14000000);
  static const hairlineDark = Color(0x1FFFFFFF);

  static const sale = Color(0xFFB3261E);
}
